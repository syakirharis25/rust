fn main() {
    let x: u64 = 45; // u64 -- u means unsigned, not a negative number, 64 bit
    let f: f32 = 6.7; // f32 -- f means floating point number, 32 bit
    let b: bool = false; // b means boolean
}
